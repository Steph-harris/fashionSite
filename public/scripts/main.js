$(document).ready(function(){
  $(".button-collapse").sideNav();

  // var options = [
  //   {selector: '.card-panel', offset: -50, callback: 'Materialize.fadeInImage(".card-panel")' }
  // ];

  // Materialize.scrollFire(options);
});